# Copy to /srv/cairn/config/.env on the Pi. Never commit the real file.
# REGISTRY/TAG are deploy-time coordinates, set by scripts/deploy.sh itself —
# they don't belong in persistent config, so they're not here.
API_TOKEN=replace-with-a-long-random-value
API_TOKEN_READONLY=
